name: zugantor_uidl
description: >
  Platform-Agnostic UI Description Language (UIDL) — core data model,
  design token resolver, UIDL validator, and component registry.
  Pure Dart. Zero Flutter dependency.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
